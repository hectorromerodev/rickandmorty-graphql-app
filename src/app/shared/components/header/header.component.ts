import { Component } from '@angular/core';

@Component({
  selector: 'app-header',
  templateUrl: './header.component.html',
  styles: [`
    .hide{
      display: none;
    }
  `]
})
export class HeaderComponent { }
